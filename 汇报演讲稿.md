# 汇报演讲稿 - Study Planner Agent 智能学习规划多智能体系统

> 建议时长：5 到 7 分钟  
> 汇报对象：课程答辩、项目展示、技术评审  
> 配套文档：`README.md`、`快速开始.md`、`项目交付清单.md`

---

## 1. 开场：为什么做这个项目

各位老师、同学好，我这次汇报的项目是 **Study Planner Agent，智能学习规划多智能体系统**。

这个项目解决的是个人学习中很常见的问题：很多人不是完全缺资料，而是不知道应该先学什么、每天怎么推进、资源怎么筛选、学到什么程度算真正掌握。

传统大模型问答可以生成一份看起来不错的学习计划，但它容易停留在“漂亮报告”层面。真正对学习者有帮助的，是一套能执行、能复盘、能自测、能根据反馈调整的学习工作流。

所以本项目的目标是：把一次学习目标输入，拆解成多智能体协作流程，最终生成可保存、可执行、可复盘的本地学习文件包。

---

## 2. 项目定位

Study Planner Agent 不是一个简单聊天机器人，而是一个面向学习规划场景的 AI Agent 应用。

它的核心输入是用户的学习目标，例如：

> 我今年 30 岁，是普通上班族，想在 6 周内系统入门股票投资，目标不是短线暴富，而是建立长期投资框架。

系统会围绕这个目标主动澄清用户基础、时间安排、学习偏好和最终产出物，然后通过多个 Agent 协同完成：

- 需求澄清
- 用户画像与长期记忆
- 资源搜索与正文核验
- 资源适配评估
- 知识结构整理
- 学习计划生成
- 多模态展示
- 反馈调整与资源重搜
- 最终文件保存与执行包导出

最终用户得到的不是一段聊天记录，而是一个本地文件夹，里面包括完整报告、图片素材、每日清单、复盘表和自测题。

---

## 3. 系统架构

项目整体采用五层架构，README 中放置了完整架构图：

1. **用户输入层**：接收学习目标、当前基础、时间安排、偏好和反馈。
2. **调度与记忆层**：负责任务路由、需求澄清、长期画像和全局记忆。
3. **搜索与分析层**：负责 Tavily、MiniMax 搜索、正文抽样核验和资源适配评分。
4. **规划与优化层**：负责结构化计划生成、多模态展示和反馈后的动态调整。
5. **输出交付层**：负责最终报告、图片、资源卡片、执行包和本地文件夹。

这个架构的关键点是：主链路固定，保证稳定性；同时在记忆、资源、反馈和交付环节做增强，让系统更像一个真正能协作的学习规划 Agent。

---

## 4. Agent 三要素

课程要求 AI Agent 包含记忆、规划、工具三个基本要素，本项目都做了落地。

第一是 **记忆**。  
系统有 `SessionState` 保存本轮上下文，也有 `memory_store/` 保存长期画像。更重要的是，项目把 `soul-instr/` 改造成了记忆规则库，包含：

- `memory-policy.md`：规定什么该记、什么不该记。
- `profile-schema.md`：规定画像 JSON 字段。
- `memory-merge-rules.md`：规定历史画像、全局记忆和本轮回答如何合并。

`ProfileMemoryAgent` 会读取这些 Markdown 规则，避免保存 API Key、密码等敏感信息，也避免把一次性反馈误写成长期偏好。

第二是 **规划**。  
系统固定串联 9 个 Agent，最终由 `PlanGenerationAgent` 输出长期目标、阶段目标、逐周计划、每日建议和里程碑。反馈后如果计划变化，多模态展示也会重新渲染相关内容。

第三是 **工具**。  
项目通过 `ToolRegistry` 接入搜索、链接校验、网页正文抓取、图片生成等工具。资源搜索 Agent 会调用 Tavily 和 MiniMax 搜索，资源评估 Agent 会用 `fetch_url` 抽样核验正文，多模态展示 Agent 会调用图片生成工具。

---

## 5. 核心流程

整个系统的主流程可以概括为九步：

第一步，用户输入学习目标。  
第二步，系统生成澄清问题，像 Claude 一样逐题询问用户。  
第三步，画像与记忆 Agent 结合全局记忆、历史画像和本次回答，形成用户画像。  
第四步，资源搜索 Agent 使用 Tavily 和 MiniMax 联网搜索获取候选资源。  
第五步，资源评估 Agent 对资源进行正文抽样、难度评估和学习适配分析。  
第六步，知识整理 Agent 把目标领域拆成知识结构。  
第七步，计划生成 Agent 生成阶段目标、周计划、每日任务和里程碑。  
第八步，多模态展示 Agent 生成知识图、路线图、流程图、封面图和产出物图。  
第九步，用户反馈调整，满意后保存最终文件。

Web 端通过 SSE 实时事件流显示 Agent 运行状态，因此用户不会觉得“点了提交没有反应”。每个阶段正在做什么、是否完成、是否失败，都能在聊天区域看到。

---

## 6. 三个主要亮点

第一个亮点是 **学习执行包**。

传统学习计划通常只输出一份报告，而本项目会在用户确认保存后生成完整执行包：

```text
plans/<学习目标>/
  <model>-multiagent.md
  assets/
  <model>-package/
    plan.md
    daily-checklist.md
    review-log.md
    quiz.md
```

其中 `daily-checklist.md` 用来每天打勾推进，`review-log.md` 用来复盘，`quiz.md` 用来自测。这样用户第二天真正打开的不是一份静态报告，而是一个可以执行的任务系统。

第二个亮点是 **资源核验与反馈重搜**。

资源评估不是只看标题和摘要，而是会对部分候选资源抓取正文片段，再判断语言、费用、难度、相关度和使用建议。用户如果反馈“换成中文资源”“想要实战项目”“太难了”，系统会重新搜索和评估资源，而不是只改报告文字。

第三个亮点是 **M3 多模态图片生成**。

项目支持在设置中选择图片生成模型，默认使用 MiniMax-M3，也可以切回 image-01 或填写自定义模型 ID。知识地图、技能依赖图、执行流程图、练习循环图、学习路线图和产出物清单都会优先把结构数据转成提示词，让 M3 生成对应 PNG 图片；如果图片模型失败或没有 Key，则自动回退本地 SVG，保证报告不缺图。

---

## 7. Web 端体验

Web 端参考了 ChatGPT、Claude 和 Codex 的交互方式。

左侧是学习计划列表，用户可以查看历史计划、删除计划，也可以新建学习目标。  
中间是智能规划对话区，负责输入目标、回答澄清问题、查看 Agent 状态和提交反馈。  
右侧是本地文件夹工作区，用户保存后可以直接打开生成目录，再用 Typora 或其他 Markdown 工具查看文件。

设置入口放在左下角，支持配置：

- 主模型：MiniMax、Kimi、DeepSeek
- 图片生成模型：默认 MiniMax-M3，可切 image-01 或自定义
- 搜索能力：Tavily 和 MiniMax 联网搜索
- 全局记忆：用于记录长期学习偏好
- 外观模式：白天、夜间或跟随系统
- 画布风格：纯净、网格、纸张、纹理

保存完成后，系统会自动回到新建学习规划状态，方便用户继续创建新的学习方向。

---

## 8. 技术实现与稳定性

后端主要使用 Python 标准库和轻量依赖实现，Web 服务使用本地 HTTP 服务承载页面与 API。

核心 Agent 位于 `common/agent/multi/agents.py`，流程编排位于 `common/agent/multi/pipeline.py`，Web 接口位于 `common/agent/web_app.py`。

项目有几个稳定性设计：

- 记忆规则由 `soul-instr/` Markdown 文件维护，画像 Agent 运行时读取。
- 所有文件导出都是附加产物，不影响原始报告保存。
- 搜索失败时回退模型生成，资源抓取失败时按标题和摘要评估。
- 图片生成优先使用 M3，失败时回退 SVG。
- 自测题生成失败时重试和容错解析，仍失败则写入占位说明。
- Web 长任务通过 SSE 推送进度，避免前端长时间无响应。

这些设计保证了即使 API、网络或图片生成出现波动，学习计划主流程仍能尽量完成。

---

## 9. 测试与验收

项目提供了单元测试和静态检查，覆盖了以下能力：

- 学习执行包生成
- 自测题 JSON 解析与降级
- 记忆规则读取与画像保存
- 资源评估字段渲染
- 反馈重搜逻辑
- M3 图片生成配置与 PNG/SVG 降级
- Web 会话状态、SSE、保存和删除计划
- 前端关键元素和交互逻辑

推荐验收流程是：

1. 运行 `python web_app.py` 启动 Web 端。
2. 在设置里配置主模型、图片模型、搜索 Key 和全局记忆。
3. 输入一个真实学习目标。
4. 回答澄清卡片中的问题。
5. 观察 Agent 阶段状态是否实时展示。
6. 生成草稿后提交一次反馈。
7. 点击保存。
8. 打开 `plans/<目标>/`，检查报告、图片、assets 和执行包是否完整。

---

## 10. 总结

总结来说，这个项目完成了从“生成一份学习计划”到“构建一个学习规划智能体”的升级。

它的价值主要体现在三点：

第一，学习计划更可执行，有每日清单、复盘表和自测题。  
第二，学习资源更可信，有搜索、正文核验、评分和使用建议。  
第三，交互体验更完整，有澄清问题、实时状态、长期记忆、反馈调整、多模态图片和本地文件交付。

后续如果继续扩展，可以加入多用户持久化、学习进度追踪、日历提醒、资料上传和 RAG 问答，让它从规划工具进一步发展成完整的个人学习系统。

以上就是我的项目汇报，谢谢大家。
