# Study Planner Agent - 快速开始指南

## 运行演示（无需 API Key）

```powershell
cd D:\agent-dev\study-planner-agent
python demo.py
```

这将演示 Agent 的核心功能，不需要任何 API key。

## 运行完整应用

### 1. 安装依赖

```powershell
cd D:\agent-dev\study-planner-agent
python -m pip install -r requirements.txt
```

### 2. 配置 API Key

有两种方式：

#### 方式一：环境变量

```powershell
# 设置 Moonshot (Kimi) API Key
$env:MOONSHOT_API_KEY = "sk-xxxx"

# 或设置 DeepSeek API Key
$env:DEEPSEEK_API_KEY = "sk-xxxx"
```

#### 方式二：运行时输入

直接运行程序，按提示输入 API Key。

### 3. 启动应用

#### 使用 Kimi (推荐)

```powershell
python providers/kimi.py
```

#### 使用 DeepSeek

```powershell
python providers/deepseek.py
```

## 使用流程

### 模式 1：学习目标设定

适合：刚开始学习某领域，想明确目标

```
1. 启动程序，选择模式 1
2. 输入学习目标（可多行）
3. 系统自动：
   - 理解你的目标
   - 评估你的水平
   - 推荐学习资源
4. 生成结构化目标建议
5. 可多轮对话细化
```

### 模式 2：学习计划制定

适合：有明确目标，需要详细计划

```
1. 启动程序，选择模式 2
2. 输入主要学习目标
3. 选择当前水平（初学/中级/高级）
4. 系统自动：
   - 分解大目标为小任务
   - 安排时间表
   - 设置里程碑
   - 推荐资源
5. 生成完整学习计划
6. 可多轮对话调整
```

## 常见问题

### Q: API Key 从哪获取？

- **Moonshot (Kimi)**: https://platform.moonshot.cn
- **DeepSeek**: https://platform.deepseek.com

### Q: 支持哪些模型？

- Kimi: kimi-k2.6, kimi-k2, kimi-lite
- DeepSeek: deepseek-v4-flash, deepseek-v4, deepseek-coder

### Q: 会话记录保存在哪？

所有会话自动保存在 `plans/` 目录下，按学习主题分目录存储。

### Q: 如何继续之前的对话？

下次运行同一主题时，选择 Resume 即可继续。

## 项目结构

```
study-planner-agent/
├── README.md              # 项目文档
├── 快速开始.md            # 本指南
├── 汇报演讲稿.md          # 课程汇报演讲稿
├── requirements.txt       # 依赖
├── demo.py               # 演示脚本
│
├── soul-instr/           # Agent 记忆
│   ├── prompt.md         # 身份定位
│   ├── mode1-goal.md     # 模式1指南
│   └── mode2-plan.md     # 模式2指南
│
├── common/               # 核心代码
│   ├── agent/           # Agent 核心
│   │   ├── memory.py    # 记忆管理
│   │   ├── planner.py   # 规划模块
│   │   ├── executor.py  # 执行引擎
│   │   ├── orchestrator.py # 主流程
│   │   └── tools/       # 工具集
│   ├── providers/       # LLM 适配
│   ├── cli_menu.py      # 菜单
│   └── storage.py       # 存储
│
├── providers/           # 入口脚本
│   ├── kimi.py
│   └── deepseek.py
│
└── plans/               # 学习计划输出
    └── {主题名}/
        └── {model}.md
```

## 技术栈

- Python 3.11+
- httpx (HTTP 客户端)
- 支持多种 LLM 后端 (Kimi, DeepSeek)

## 团队分工建议

| 角色 | 职责 | 备注 |
|------|------|------|
| 项目负责人 | 架构设计、进度把控 | 1人 |
| 后端开发 | Agent 核心、工具实现 | 2人 |
| 前端/演示 | CLI 界面优化、演示 | 1人 |
| 文档/汇报 | README、演讲稿、PPT | 1人 |

## 后续改进建议

1. **增加更多工具**：日历集成、邮件提醒等
2. **支持语音输入**：提升交互体验
3. **数据可视化**：图表展示学习进度
4. **多语言支持**：服务全球用户
5. **Web 界面**：开发网页版应用
