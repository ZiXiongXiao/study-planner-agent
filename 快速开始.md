# 快速开始

这份文档用于从零启动 Study Planner Agent，并跑完一次完整的“学习目标 -> 澄清 -> 多智能体生成 -> 反馈 -> 保存 -> 执行包导出”流程。

## 1. 准备环境

推荐环境：

- Python 3.11+
- Windows PowerShell
- 至少一个主模型 API Key：MiniMax、Kimi 或 DeepSeek

进入项目目录：

```powershell
cd D:\agents\study-planner-agent-main
```

安装依赖：

```powershell
python -m pip install -r requirements.txt
```

当前项目只依赖：

```text
httpx>=0.27.0
```

## 2. 启动 Web 工作台

推荐使用 Web 工作台：

```powershell
python web_app.py
```

默认访问：

```text
http://127.0.0.1:8765
```

如果端口被占用：

```powershell
python web_app.py --port 8770
```

打开页面后，左下角点击“设置”，配置以下内容：

| 配置项 | 必填 | 说明 |
| --- | --- | --- |
| 主模型服务商 | 是 | MiniMax / Kimi / DeepSeek |
| 主模型 API Key | 是 | 用于需求澄清、资源评估、计划生成、反馈调整 |
| 图片生成模型 | 否 | 默认 MiniMax-M3，可切换 image-01 或自定义模型 ID |
| MiniMax 图生成 Key | 否 | 用于知识图、路线图、流程图、封面图、成果图、产出物清单 PNG |
| Tavily 搜索 Key | 否 | 用于真实联网搜索学习资源 |
| 全局记忆 | 否 | 跨计划生效，例如“优先中文免费资源，偏实战项目” |

保存设置后即可开始。

## 3. 推荐测试提示词

可以先用下面这个学习目标测试完整链路：

```text
我今年 30 岁，是普通上班族，想在 8 周内系统入门 AI Agent 应用开发。目标不是只看概念，而是能独立做出一个可用的小项目，比如个人学习规划助手或资料整理助手。我目前会一点 Python，但不熟悉 LangChain、Dify、RAG、工具调用和前后端部署。希望计划偏实战，每周学习时间大概 6-8 小时，优先中文免费资源，内容不要太难，要有阶段性作品、自测题和复盘安排。
```

这个提示词会触发：

- 澄清问题
- 用户画像与长期记忆
- 资源搜索与评估
- 项目导向计划生成
- 多模态展示
- 学习执行包导出
- quiz 自测题生成

## 4. 完整使用流程

### 第一步：输入学习目标

在中间输入框输入学习目标，点击“生成澄清问题”。

### 第二步：回答澄清问题

系统会像 Claude 一样在输入框上方显示单题卡片：

- 可点击选项
- 可跳过
- 可直接在输入框中自定义回答
- 选项较多时中间区域可滚动

最后一题提交后，系统进入多智能体运行态。

### 第三步：等待 Agent 运行

页面会实时显示运行状态：

```text
画像与记忆 -> 资源搜索 -> 资源评估 -> 知识整理 -> 计划生成 -> 多模态展示
```

左侧聊天区会显示 Agent 日志，右侧会逐步更新计划预览和资源卡片。

### 第四步：反馈调整

草稿生成后，可以继续在输入框反馈，例如：

```text
换成中文免费资源，多给 B 站和实战项目。
```

或：

```text
时间太紧，把 8 周压缩成 4 周，但保留最终项目。
```

资源类反馈会触发重新搜索和重新评估，不只是改文字。

### 第五步：保存最终文件

满意后点击“保存最终文件”。保存成功后：

- 系统会写入 `plans/<目标>/`
- 左侧计划列表自动刷新
- 页面自动回到“新建学习规划”状态
- 可以直接开始下一个学习目标

## 5. 输出文件说明

保存后会生成：

```text
plans/<学习目标>/
  <model>-multiagent.md
  assets/
  <model>-package/
    plan.md
    daily-checklist.md
    review-log.md
    quiz.md
```

文件用途：

| 文件 | 说明 |
| --- | --- |
| `<model>-multiagent.md` | 完整最终报告，包含计划、资源、图片和 Agent 协作日志 |
| `assets/` | 封面图、结构图、路线图、产出物图等 |
| `plan.md` | 最终计划副本，适合用 Typora 打开 |
| `daily-checklist.md` | 每日/每周可勾选任务清单 |
| `review-log.md` | 复盘日志模板 |
| `quiz.md` | 阶段自测题与折叠答案 |

## 6. 可选：命令行运行

也可以使用 provider 入口运行命令行版本：

```powershell
python providers/minimax.py
python providers/kimi.py
python providers/deepseek.py
```

命令行会按提示输入 API Key、选择模型、输入目标、回答澄清问题并保存结果。

## 7. 可选：配置环境变量

如果不想每次在页面中填 key，可以使用环境变量：

```powershell
$env:MINIMAX_API_KEY = "sk-..."
$env:MOONSHOT_API_KEY = "sk-..."
$env:DEEPSEEK_API_KEY = "sk-..."
$env:TAVILY_API_KEY = "tvly-..."
```

说明：

| 变量 | 用途 |
| --- | --- |
| `MINIMAX_API_KEY` | MiniMax 主模型、图生成、MiniMax 搜索默认复用 |
| `MOONSHOT_API_KEY` | Kimi |
| `DEEPSEEK_API_KEY` | DeepSeek |
| `TAVILY_API_KEY` | Tavily 搜索 |
| `MINIMAX_IMAGE_MODEL` | 可选，覆盖图片生成模型；默认 `m3` |
| `MINIMAX_IMAGE_URL` | 可选，覆盖图片生成接口地址 |
| `MINIMAX_SEARCH_API_KEY` | 可选，单独给 MiniMax Web Search MCP 使用 |
| `MINIMAX_SEARCH_CMD` | 可选，覆盖 MCP 启动命令 |

## 8. 可选：MiniMax Web Search MCP

资源搜索会优先合并 Tavily 与 MiniMax Web Search MCP 的结果。MiniMax MCP 可帮助补充中文互联网和国内学习资源，例如 B 站教程。

这是可选增强，未安装时系统会自动跳过。

基础条件：

- 本机可运行 `uvx`
- 可运行 `minimax-coding-plan-mcp`
- 已配置 `MINIMAX_API_KEY` 或 `MINIMAX_SEARCH_API_KEY`

如果你的 MCP 启动命令不同，可以覆盖：

```powershell
$env:MINIMAX_SEARCH_CMD = "uvx minimax-coding-plan-mcp -y"
```

## 9. 常见问题

### 搜索失败但计划还生成了，正常吗？

正常。搜索接口、目标网站或网络可能出现 SSL 超时、EOF 中断等问题。系统会保留已有结果或回退到纯模型生成，主流程不会因此崩掉。

### 没有 MiniMax 图生成 Key 可以用吗？

可以。没有图生成 Key 时，AI PNG 会跳过，但学习路线图、流程图等本地 SVG 仍会生成。配置 Key 后，结构图会优先用 M3 根据提示词生成 PNG。

### quiz.md 出现占位怎么办？

`quiz.md` 是最后一次 LLM 调用。如果模型返回无法解析的 JSON，系统会写占位说明而不影响其他文件。可以重新运行，或后续在代码里增加分阶段重试策略。

### 保存后为什么页面清空了？

这是设计行为：一次聊天对应一次最终学习计划。保存后系统会冻结最终结果并导出所有文件，然后回到新建状态，方便继续规划新的学习方向。已保存计划在左侧列表中。

## 10. 验证命令

开发或交付前建议运行：

```powershell
$env:PYTHONPATH='.'
$env:PYTHONIOENCODING='utf-8'
python -m unittest discover -s tests -p "test_*.py"
python -m py_compile common\agent\multi\agents.py common\agent\tools\web_search.py common\agent\web_app.py web_app.py
node --check web\app.js
```
