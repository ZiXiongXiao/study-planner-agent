# 项目交付清单

## 项目名称

Study Planner Agent - 智能学习规划多智能体系统

## 交付摘要

本项目已经从早期“单 Agent + 工具流水线”升级为本地 Web 工作台驱动的多智能体学习规划系统。当前版本具备端到端能力：

```text
学习目标输入
-> 需求澄清
-> 用户画像与长期记忆
-> 联网资源搜索
-> 资源正文核验与评估
-> 知识体系整理
-> 学习计划生成
-> 多模态展示
-> 反馈调整/资源重搜
-> 最终报告与学习执行包导出
```

核心交付物包括：

- 本地 Web 工作台
- 命令行多模型入口
- 9 个 Agent 固定主链路
- Tavily + MiniMax 搜索融合
- 资源评估正文抽样核验
- MiniMax-M3 图片生成与本地 SVG 兜底
- 学习执行包导出
- 已保存计划列表与本地文件夹入口
- 单元测试与静态验证命令

## 架构图

![学习规划多智能体系统设计](docs/assets/architecture.png)

## 交付文件清单

### 1. 核心后端

| 文件 | 说明 |
| --- | --- |
| `common/agent/multi/agents.py` | 9 个核心 Agent：路由、澄清、画像、搜索、评估、整理、计划、展示、反馈 |
| `common/agent/multi/pipeline.py` | 命令行多智能体主流程、最终报告保存 |
| `common/agent/multi/package.py` | 学习执行包导出：plan/checklist/review-log/quiz |
| `common/agent/multi/state.py` | 多 Agent 共享状态 |
| `common/agent/multi/display.py` | Markdown 资源卡片、表格、路线图等展示辅助 |
| `common/agent/multi/memory_store.py` | 长期画像记忆与记忆规则加载 |
| `common/storage.py` | `plans/<目标>/` 安全目录解析 |

### 2. 工具层

| 文件 | 说明 |
| --- | --- |
| `common/agent/tools/web_search.py` | Tavily 搜索、MiniMax MCP 搜索、URL 存活校验、GitHub/arXiv 补充 |
| `common/agent/tools/sources.py` | fetch_url、GitHub Search、arXiv Search |
| `common/agent/tools/image_gen.py` | MiniMax-M3 / image-01 可选图片生成与下载保存 |
| `common/agent/tools/diagram_render.py` | 图片生成失败时的本地 SVG 兜底渲染 |
| `common/agent/tools/registry.py` | ToolRegistry 与 ToolResult 统一工具契约 |

### 3. Web 工作台

| 文件 | 说明 |
| --- | --- |
| `common/agent/web_app.py` | 本地 HTTP API、会话管理、SSE 事件流、保存/打开/删除计划 |
| `web_app.py` | Web 服务启动入口 |
| `web/index.html` | Web 页面结构 |
| `web/app.js` | 前端交互、SSE、保存后重置、计划列表、主题设置 |
| `web/styles.css` | 响应式布局、日夜间模式、画布风格、可滚动卡片 |

### 4. Provider

| 文件 | 说明 |
| --- | --- |
| `providers/minimax.py` / `providers/minimax_impl.py` | MiniMax 模型入口与 OpenAI 兼容 API 适配 |
| `providers/kimi.py` / `providers/kimi_impl.py` | Kimi 模型入口 |
| `providers/deepseek.py` / `providers/deepseek_impl.py` | DeepSeek 模型入口 |
| `common/providers/base.py` | Provider 路径与基础适配辅助 |

### 5. 文档与资源

| 文件 | 说明 |
| --- | --- |
| `README.md` | 项目总说明 |
| `快速开始.md` | 安装、配置、运行、验证指南 |
| `项目交付清单.md` | 当前交付文档 |
| `soul-instr/memory-policy.md` | 画像记忆边界与安全规则 |
| `soul-instr/profile-schema.md` | 用户画像 JSON 字段约束 |
| `soul-instr/memory-merge-rules.md` | 历史画像/全局记忆/本轮回答合并策略 |
| `docs/assets/architecture.png` | 项目架构图 |
| `requirements.txt` | Python 依赖 |

### 6. 测试

| 文件 | 覆盖点 |
| --- | --- |
| `tests/test_web_app.py` | Web API、SSE、前端静态能力、保存后重置、删除计划 |
| `tests/test_learning_package.py` | 学习执行包四文件导出、quiz 降级 |
| `tests/test_resource_and_feedback.py` | 资源正文抓取、资源卡片字段、反馈重搜 |
| `tests/test_multimodal_images.py` | SVG/PNG 多模态生成、产出物 PNG 优先、无 key 降级 |
| `tests/test_minimax_search.py` | MiniMax 搜索与 Tavily 搜索结果融合 |
| `tests/test_image_gen.py` | MiniMax-M3 / image-01 图片生成与下载保存 |
| `tests/test_diagram_render.py` | 本地 SVG 结构图渲染 |
| `tests/test_pipeline_save.py` | 最终报告图片引用写入 |

## 功能验收清单

### A. Web 工作台

- [x] 可通过 `python web_app.py` 启动本地 Web 服务。
- [x] 可在设置中选择 MiniMax、Kimi、DeepSeek。
- [x] 可配置主模型 Key、MiniMax 图生成 Key、Tavily Key、全局记忆。
- [x] 支持日间/夜间模式。
- [x] 支持画布背景风格：纯净、网格、纸张、纹理。
- [x] 左右分栏可拖拽，且不会拖到不可用宽度。
- [x] 澄清问题显示为单题卡片，选项可滚动。
- [x] Agent 运行状态通过 SSE 实时显示。
- [x] Agent 文本日志显示在聊天区域。
- [x] 草稿生成后反馈输入框和保存按钮可达。
- [x] 保存成功后自动回到新建规划状态。
- [x] 左侧已保存计划列表会刷新。
- [x] 已保存计划可删除，并同步删除本地文件夹。

### B. 多智能体主链路

- [x] 任务编排/路由 Agent 能识别领域与关键词。
- [x] 需求澄清 Agent 能生成 3 到 4 个选择题。
- [x] 画像与记忆 Agent 能保存长期画像。
- [x] 画像与记忆 Agent 会读取 `soul-instr` 记忆规则，避免保存敏感信息或错误沉淀临时反馈。
- [x] 资源搜索 Agent 支持 Tavily、MiniMax MCP、GitHub、arXiv 和模型兜底。
- [x] 资源评估 Agent 会抽样 fetch 正文。
- [x] 知识整理 Agent 输出结构化知识体系。
- [x] 计划生成 Agent 输出结构化 JSON 计划。
- [x] 多模态展示 Agent 渲染 Markdown、SVG、PNG。
- [x] 反馈调整 Agent 能修改计划。
- [x] 资源类反馈能触发重新搜索与重新评估。

### C. 输出文件

- [x] 保存 canonical 报告：`<model>-multiagent.md`。
- [x] 保存图片资产目录：`assets/`。
- [x] 导出 `plan.md`。
- [x] 导出 `daily-checklist.md`。
- [x] 导出 `review-log.md`。
- [x] 导出 `quiz.md`。
- [x] quiz 生成失败时写占位，不影响其他文件。
- [x] 执行包中图片路径可从 `../assets/` 正确引用。

### D. 图片与多模态

- [x] 知识地图、技能依赖、执行流程、练习循环、学习路线等结构图优先由 M3 根据提示词生成 PNG。
- [x] 有 MiniMax 图生成 Key 时，封面/成果/具象图可生成 PNG。
- [x] 产出物清单优先生成 `deliverables.png`。
- [x] 图片模型失败或无 key 时回退 SVG，保证报告不缺图。
- [x] 封面/成果等氛围图默认无文字，结构图提示词允许简短标签。
- [x] 图片保存到 `plans/<目标>/assets/`。

### E. 资源搜索与核验

- [x] Tavily 搜索支持多类型查询。
- [x] MiniMax Web Search MCP 可作为补充来源。
- [x] GitHub/arXiv 公开 API 可补充项目与论文。
- [x] URL 存活校验仅丢弃明确 404/410，网络异常保守保留。
- [x] 资源评估输出语言、费用、难度、相关度和用法建议。
- [x] 搜索/抓取失败不阻断主流程。

## 运行说明

### 推荐：Web 工作台

```powershell
cd D:\agents\study-planner-agent-main
python -m pip install -r requirements.txt
python web_app.py
```

访问：

```text
http://127.0.0.1:8765
```

端口冲突时：

```powershell
python web_app.py --port 8770
```

### 命令行入口

```powershell
python providers/minimax.py
python providers/kimi.py
python providers/deepseek.py
```

## 推荐演示脚本

演示时可使用下面的学习目标：

```text
我今年 30 岁，是普通上班族，想在 8 周内系统入门 AI Agent 应用开发。目标不是只看概念，而是能独立做出一个可用的小项目，比如个人学习规划助手或资料整理助手。我目前会一点 Python，但不熟悉 LangChain、Dify、RAG、工具调用和前后端部署。希望计划偏实战，每周学习时间大概 6-8 小时，优先中文免费资源，内容不要太难，要有阶段性作品、自测题和复盘安排。
```

建议演示路径：

1. 打开 Web 工作台。
2. 展示设置：主模型、搜索 Key、图生成 Key、全局记忆、外观。
3. 输入学习目标。
4. 回答澄清问题。
5. 观察 Agent Run 实时状态。
6. 展示计划预览与资源卡片。
7. 输入反馈：“换成中文免费资源，多给 B 站和实战项目。”
8. 展示资源重搜后的更新。
9. 点击保存最终文件。
10. 打开 `plans/<目标>/`，展示报告、assets 和 package。

## 测试命令

```powershell
$env:PYTHONPATH='.'
$env:PYTHONIOENCODING='utf-8'
python -m unittest discover -s tests -p "test_*.py"
python -m py_compile common\agent\multi\agents.py common\agent\tools\web_search.py common\agent\web_app.py web_app.py
node --check web\app.js
```

当前已覆盖的关键测试：

- Web 会话与 SSE 后台任务
- 前端静态结构和布局能力
- 保存后自动新建态
- 计划删除与列表刷新
- 学习执行包导出
- quiz 失败降级
- 资源正文 fetch 与字段渲染
- 反馈重搜
- 多模态 SVG/PNG 生成
- MiniMax 搜索融合

## 评分标准对照

| 评分维度 | 项目体现 |
| --- | --- |
| 选题定位 | 面向个人学习者、学生、职场转型者和自学者的学习规划 Agent |
| 应用价值 | 将“漂亮报告”转化为可执行学习任务系统，降低自学组织成本 |
| 技术完整性 | 9 个 Agent、工具注册、联网搜索、正文核验、记忆、反馈循环、Web UI |
| 创新性 | 资源类反馈触发重搜，学习执行包，结构图与 AI 图分工，MiniMax + Tavily 搜索融合 |
| 易用性 | 本地 Web 工作台、实时状态、保存计划列表、本地文件夹入口 |
| 稳定性 | 搜索失败降级、图生成失败降级、quiz 失败占位、保存流程 try/except |
| 可验证性 | 56 个单元测试覆盖主要链路 |

## 已知限制与降级策略

| 情况 | 策略 |
| --- | --- |
| Tavily SSL 超时或 EOF | 保留已有结果，或回退模型生成 |
| MiniMax MCP 未安装 | 跳过 MiniMax 搜索，不影响 Tavily |
| 图生成 Key 缺失 | 跳过 PNG，保留 SVG |
| 图片模型生成失败 | 回退生成对应 SVG |
| quiz JSON 解析失败 | 写占位说明，不影响其他文件 |
| API 内容风控 | 前端显示错误，用户可调整提示词或换模型 |

## 后续优化建议

1. 引入任务队列与持久化任务恢复，避免服务重启丢失运行中会话。
2. 增加用户级数据库，将 `web_data/settings.json` 与 `plans/` 升级为多用户存储。
3. 为 MiniMax MCP 搜索增加更完整的安装检测与 UI 提示。
4. 增加 Typora 一键打开指定 Markdown 文件，而不仅是打开文件夹。
5. 增加计划执行进度追踪，将 `daily-checklist.md` 勾选状态同步回 Web。
6. 增加资源收藏、替换和屏蔽功能。
7. 增加可导出 PDF/Word 的交付格式。

## 交付结论

当前项目已满足“可运行、可演示、可保存、可验证”的交付标准。核心链路稳定，关键失败点均有降级策略，文档与测试已经覆盖当前功能状态。项目可直接用于课程展示，也可继续作为个人学习规划产品原型迭代。
